// Project Configuration
// Change parameters for testing the project in local environment

const useDataFromMock = false // if false, use API data
const userId = 12 // userId in testing mode are 12 for Karl or 18 for Cecilia

export { useDataFromMock, userId }
